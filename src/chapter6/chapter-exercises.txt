Multiple choice

1. The Eq class
  a) includes all types in Haskell.
  b) is the same as the Ord class.
v c) makes equality tests possible.
  d) only includes numeric types.

2. The type class Ord
  a) allows any two values to be compared.
v b) is a subclass of Eq.
  c) is a superclass of Eq.
  d) has no instance for Bool.

3. Suppose the type class Ord has an operator >. What is the type
   of >?
v a) Ord a => a -> a -> Bool
  b) Ord a => Int -> Bool
  c) Ord a => a -> Char
  d) Ord a => Char -> [Char]

4. In x = divMod 16 12
  a) the type of x is Integer.
  b) the value of x is undecidable.
v c) the type of x is a tuple.
  d) x is equal to 12 / 16.

5. The type class Integral includes
v a) Int and Integer numbers.
  b) integral, real, and fractional numbers.
  c) Schrodinger's cat.
  d) only positive numbers.
